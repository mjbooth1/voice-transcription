# Additional packages needed for system-wide transcription
# Install these in your existing venv312 environment

# Windows toast notifications (use latest stable)
windows-toasts>=1.1.0

# Clipboard management
pyperclip>=1.8.2

# Keyboard/mouse automation for pasting
pynput>=1.7.6

# Socket communication (already included in Python standard library)
# All other dependencies (faster-whisper, sounddevice, etc.) already in main requirements.txt
